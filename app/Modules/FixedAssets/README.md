# FixedAssets Module

Purpose: Own fixed asset categories, asset register, capitalization, disposal, and fixed asset reports.

Route ownership: `/fixed-assets/*`.

Models remain in `app/Models/Tenant` during the current modular monolith phase.
